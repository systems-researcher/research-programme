<!--
Copyright (c) 2026 Jason D. Gower
SPDX-License-Identifier: CC-BY-4.0
-->

# Execution log — ontology repo, phases 1 and 2

The controller's ledger from the subagent-driven execution of
`2026-08-21-ontology-repo-phase-1-2.md`. Preserved because it carries every ruling
made on the author's behalf and every finding triaged as deferred — both of which
Phases 3 to 6 need and neither of which the commit history states in one place.

Result: 13 tasks, 21 commits, 101 tests, 8 fix rounds, no Critical findings at the
final whole-branch review.

---

Spec: docs/plans/2026-08-21-epistemic-adequacy-ontology-split-design.md (read; binding authority)
Plan hardened over 5 adversarial review rounds, 54 findings, 0 false positives.
Round 5 verified a verbatim run of all 13 tasks completes with zero unauthorised
edits: 88 tests, no drift, shapes hold over 3 instances, all 7 exit criteria.

## Ruling: no git worktree

The plan creates a NEW sibling repository at `GitHub/epistemic-adequacy-ontology`
by cloning `epistemic-adequacy-metamodel`. It modifies neither `research-programme`
(this repo, where the plan lives) nor the source repo — a clone reads the source
and writes only to the new directory. A worktree of `research-programme` would
isolate nothing that is being written.
Cost if wrong: the new directory is a fresh clone; `rm -rf` reverses it entirely.

## Pre-flight conflict scan

| Shared file / interface | Tasks | Produces vs consumes | Finding |
|---|---|---|---|
| `model/ontology.yaml` | 2,3,4,6 | T2 abstracts 7 types + header; T3 removes RevisionSchemeKind; T4 removes 0-2 enums; T6 rewrites member docs | Clean — disjoint edits, strictly ordered |
| `tests/test_load.py` | 3,4 | T3 sets enum count 12→11; T4 sets →9 if it deleted | Clean — T4's value supersedes T3's, both stated |
| `src/eaont/load.py` | 1,2 | T1 renames package; T2 adds REFUSED_PREFIXES/ABSTRACT_REFS | Clean |
| `src/eaont/cli.py` | 1,10 | T1 writes imports+TARGETS; T10 adds `validate` above `return generate()` | Clean — T10 states the ordering |
| `src/eaont/generate/*.py` | 1,2 | T1 seds `eamm`; T2 S5b/6b edit comment + provenance, then regenerate | Clean — T2 S6b explicitly ordered before S7 |
| `ontology/*.ttl` (generated) | 2,3,4,6 | Regenerated by each; byte-compared | Clean |
| `trace/spec-trace.yaml` | 6,7 | T6 creates with `entities: {}`; T7 populates it | Clean — T6 precedes T7 |
| `tests/test_no_binding_concerns.py` | 3,4 | T3 creates; T4 extends | Clean |
| `tests/test_release.py` | 13 S1c, 13 S4 | S1c writes a test into it; S4 says "Create" it | **CONFLICT — see ruling below** |
| `bindings/reference/instances/` | 8,12 | T8 writes 2; T12 adds a 3rd; T10 globs `*.json` | Clean — T10 states 2, exit criteria state 3 |
| `docs/adr-010-test.md` | 12,13 | T12 writes `VERDICT:` line; T13 S4 parses it | Clean — T12 precedes T13 |
| `tests/fixtures/pre-split-*.txt` | 5 | Baselined from the untouched SOURCE repo at `../epistemic-adequacy-metamodel` | Clean — source is never modified |
| CC-BY-4.0 header on `docs/*.md` | 7,8,11,13 | T13 S4's release test requires it on every doc | Clean — each creating step states it |
| Global constraint: no KerML/SysML in `model/` | 2 | T2 makes it a load error + a test | Clean |

Per-task self-agreement: all 13 checked — tests specified against code specified,
files created against files later touched. Only the Task 13 row below disagreed.

## Ruling: Task 13 Step 4 appends, it does not overwrite

Step 1c places `test_entities_doc_covers_every_definition` in `tests/test_release.py`;
Step 4, later in the same task, says "Create `tests/test_release.py`". Taken
literally the second overwrites the first and Step 1c's coverage test is lost
silently — the suite would still be green, having quietly stopped checking
`docs/entities.md`.
Ruling: Step 4 APPENDS to the file Step 1c created, sharing its imports. The task
is complete only when `tests/test_release.py` contains BOTH Step 1c's test and all
of Step 4's. Carried into the Task 13 dispatch.
Cost if wrong: an extra test file or a duplicated import block — visible in review,
trivial to fix.

## Progress

Ruling: review packages are built with `git -C <new repo>` rather than the SDD script,
  because the work lives outside research-programme. Pure deletions are listed by name
  rather than diffed (25k lines of pre-split content teaches a reviewer nothing).
  Cost if wrong: a reviewer misses a defect hidden inside a deleted file's content.

Task 1: complete (commits 2ee9746..2f6d595, spec OK, quality approved)
Task 1: resolved (cannot-verify): `rm -rf node_modules .pilot .venv build` are
  untracked build artefacts, invisible to git by construction. Nothing downstream
  reads them. Not a gap.
Task 1: minor (deferred): task-1-report.md prose says "26 named test files"; the brief
  lists 27 and 27 were deleted. Artefact correct, tally off by one.
Task 1: minor (deferred): docs/adr/006-l2-is-the-generation-source.md still describes
  generating the SysML v2 library, now deleted from this repo. Unassigned to any task.
  (The reviewer also flagged model/ontology.yaml's header — that IS assigned, to Task 2
  Step 5; the reviewer only had Task 1's brief and could not know.)

Task 2: complete (commits 2f6d595..35439e8, spec OK, quality approved)
Task 2: **the design's central claim held under execution.** Abstracting the seven
  SysML/KerML-typed parameters changed ZERO triples: ontology/ differs from the
  pre-task state by 3 added / 3 deleted lines, one authorised Step 6b provenance
  rdfs:comment per artefact. check-drift clean. Design §9.1 is now measured, not argued.
Task 2: reviewer's structural note worth keeping — REFUSED_PREFIXES only upgrades the
  ERROR MESSAGE for a language type; what actually closes the gate is _check_types'
  whitelist (`known`). A typo'd type with no matching prefix still fails, on the
  membership branch. The agnosticism gate is the whitelist, not the prefix list.
Task 2: minor (deferred): module docstrings in load.py:3, generate/owl.py:3,
  generate/shacl.py:4 still read "metamodel.yaml -> ..." — bare filename, so Step 6b's
  sed (which required the `metamodel/` prefix) did not match.
Task 2: minor (deferred): refusal tests assert only that the type string appears in the
  message, which the fallback "unknown type" error also satisfies. They catch removal of
  refusal, not loss of the explanation. Brief-verbatim, no implementer discretion.
Task 2: minor (deferred): README.md:23 still cites metamodel/metamodel.yaml as the sole
  hand-edited artefact. Task 13 Step 2 rewrites README wholesale — expected to clear.

Task 3: complete (commits 35439e8..b0b890b, spec OK, quality approved, zero findings)
Task 3: Round 5's red-commit defect is demonstrably fixed — tests/test_load.py is in the
  commit, count 12->11, working tree clean so the commit itself is green (40 passed).
Task 3: removal proved exactly scoped by line accounting, not just by the anchored grep:
  23 removed lines in epistemic-adequacy.ttl = 6 (revisionScheme property) + 12 (three
  RevisionSchemeKind individuals) + 5 (the class). Nothing unaccounted for.

Task 4: complete (commits b0b890b..23aab3a, spec OK, quality approved)
Task 4: **design §12.1's open question is settled: both are duplicates, both deleted.**
  Re-derived independently by the reviewer from clauses.yaml, not taken on trust:
  strength.minimal==must = {01,02,03,05,06} matches ProfileKind.minimal verbatim;
  governed != unbound = {01-15}; safety_critical adds {16,17,18}; EA-REQ-10's
  {governed: must_if_safety_critical, safety_critical: must} maps one-to-one onto
  declaredPerClaim/allCritical. No member adds anything the spec lacks.
  Enumerations now 9. ADR-012 written and indexed.
Task 4: minor (deferred): docs/adr/007 still names profileClaimed/criticalityPolicy (and,
  from Task 3, revisionScheme) as live parameters. Reviewer's suggested fix is better than
  editing ADR-007: add a line to ADR-012's Consequences noting ADR-007 is historical on
  this point. An edited ADR is no longer the record it was.

Task 5: complete (commits 23aab3a..4b6f757, spec OK, quality approved, zero findings)
Task 5: tests proven able to fail — reviewer independently ran three mutations against the
  generated TTL: spurious class (only test_no_class_was_added failed), unauthorised class
  removal (two tests failed), and an unauthorised PROPERTY removal with its owning class
  left intact (only the property-diff test failed, all four class-level tests passed).
  That last one proves R1's M3 finding was real: a class-only baseline is blind to it.
  Baselines byte-match a fresh capture from the pre-split source. 25 classes, 50 properties.

Task 6: complete (commits 4b6f757..746cb39, spec OK, quality approved, zero findings)
Task 6: nine member docs rewritten across two enumerations; all nine verified accurate
  against the design's canonical-field table. ReviewStateKind::promoted's EA-REQ-18 link
  MOVED to trace/spec-trace.yaml's enumeration_members, not dropped — ADR-002's rationale
  intact. Implementer self-caught two under-specified first drafts by checking the design
  doc rather than trusting the passing test.

Task 7: classification independently re-derived by an Opus reviewer — **correct on all
  thirteen**, no disagreements, and all four `serves` clause sets correct. The two
  genuinely contestable rows (Criticality, Unresolved) were probed from both sides.
Task 7: Ruling: the reviewer's Important finding conflicts with the plan's verbatim text,
  and I am overriding the plan. `test_warrant_doc_mentions_every_entity` is a bare
  substring check, so moving GovernedClaim's row from the decidability table into the
  domain table and deleting its Serves cell passes all six tests — the published document
  can contradict trace/spec-trace.yaml on the ontology's headline claim, silently. The
  plan's own docs/warrant.md says "the boundary cannot erode without a failing test",
  which the reviewer demonstrated is false as written. The spec (design §4.1) makes this
  marking the contribution, so a test that lets the artefact drift from the trace defeats
  the task's purpose. Fixing beyond the plan's literal text.
  Cost if wrong: a longer test file than the plan specifies. No change to the ontology.
Task 7: minor (deferred): warrant.md:39 GovernedClaim's cell gives only the population
  half; design §3.4 also credits `revision` with EA-REQ-13's "per revision".
Task 7: minor (deferred): warrant.md:33 Provenance's reason states an encoding choice
  ("as typed references") where every other domain row states a need.
Task 7: minor (deferred): warrant.md:40 cites ADR-007, whose table names a different five
  (07 for 05) and justifies 13 via a parameter since removed. Cite ADR-007 + ADR-012.
Task 7: fix round 1/5 (1 addressed, 0 open — warrant tables now bound to the trace by set
  equality, not substring; serves ids shape-checked; commits 50ded41..f19e765)
Task 7: mutation independently reproduced by the re-reviewer: moving GovernedClaim's row
  between tables failed the two new table-binding tests while the OLD substring test still
  passed — confirming the gap was real, not theoretical. A bogus `serves: [banana]` now
  fails too. Fix diff purely additive, 63 insertions, 0 deletions.
Task 7: complete (commits 746cb39..f19e765, review clean after 1 fix round)
Task 7: minor (deferred): test_warrant_doc_mentions_every_entity remains in the file,
  redundant and still substring-weak alone, superseded by the stronger tests. Harmless.

Task 8: Ruling: the plan itself is wrong and I am correcting it. docs/binding-contract.md
  opens "Four type mappings and three obligations" while its own heading says "The four
  obligations" and lists four. Round 2 added the fourth (supply the attachment relation)
  and updated the heading but not the intro line or the commit message. FIVE adversarial
  review rounds missed it; an implementer transcribing verbatim caught it and flagged
  rather than silently patching — the right call. This document is the ontology paper's
  stated contribution, so an internal contradiction on the count of its central list is a
  real defect. Corrected in the plan (both places) and sent to the implementer for the repo.
  Cost if wrong: one word.
Task 8: noted: binding.yaml carries SPDX MIT while model/ontology.yaml carries CC-BY-4.0.
  Deliberate and defensible — a type map is configuration, not prose — and it is the
  brief's verbatim text. No test covers it either way (Task 13's gate checks docs/ only).
Task 8: noted: bindings/reference/instances/*.json sit outside src/ so are not packaged.
  Fine — Tasks 9-10 address them by repo-relative path. Flagged for anyone running the
  CLI from an installed wheel rather than an editable clone.
Task 8: Ruling: grounded.json's first claim is wrong on the ontology's OWN rule, and I am
  overriding the plan's verbatim fixture. docs/design §3.4 names this exact claim:
  "SIC::engine::thrustSeaLevel is a rated leaf value with no upstream computation — it is
  measured, not derived — so it carries @Unresolved{field=derivation}... Emitting an
  empty-input derivation object instead would fail EA-REQ-02 at MUST." The plan's fixture
  emits exactly that empty-input object. A fixture named `grounded` whose first claim is
  ungrounded by the ontology's own rule defeats its purpose, and it is the demonstration
  that a JSON binding can carry real warrant.
  Blast radius checked before ruling: Task 9's marker-count test reads ungrounded.json
  only; shapes carry no minCount on Derivation.upstream/expression; and Task 11's CQ5 still
  returns 2 rows because _add_basis and _add_evidence create the Derivation node and the
  hasDerivation edge independently of the derivation field. Contained to one fixture.
  Cost if wrong: one fixture claim; Tasks 9-11 re-run in minutes.
Task 8: minor (deferred): test_binding_maps_nothing_else is redundant — the set-equality
  assertion in test_binding_maps_every_abstract_ref already covers extra types. Plan-verbatim.
Task 8: minor (deferred): test_one_instance_is_grounded_and_one_is_not checks filenames
  only; a content swap under the same names would pass. Plan-verbatim.
Task 8: fix round 1/5 (1 addressed — obligations count; commits 7cb988b..7925dbd)
Task 8: fix round 2/5 (1 addressed, 0 open — measured leaf now carries an unresolved
  derivation marker; commits 7925dbd..fe667d7). Re-reviewer confirmed EA-REQ-02 is
  genuinely satisfied, not dodged: the clause's own text asks for "an expression whose
  inputs are themselves resolvable claims, OR A MARKER". Claim 1 keeps verified standing,
  recorded evidence, and provenance naming agent and test method — the real warrant a
  measured leaf can carry.
Task 8: complete (commits f19e765..fe667d7, review clean after 2 fix rounds)
Task 8: Ruling: carrying the twice-raised fixture-shape gap into Task 10 rather than
  parking it. Nothing executable in this repo guards `derivation`'s internal shape, so the
  above defect was found only by a human cross-check against the design doc. Task 10 is
  the validation task and is its natural home; parked instead, a future fixture could
  reintroduce the same defect silently and no gate would notice.
  Cost if wrong: ~10 lines of test in Task 10 beyond the plan's text.

Task 9: Ruling: three Important findings, all against text I wrote during the review loop,
  all being fixed rather than parked.
  (a) lift.py's REVIEW_STATE comment claims unflattening `reviewed`->`promoted` is "sound
      rather than a guess". FALSE, and this repo's own ADR-002 says so: the flattening
      WIDENS EA-REQ-18's population to include rejected claims, so a rejected claim does
      arrive as `reviewed`. Reviewer confirmed against the toolkit's subjects.py, which
      selects on `== "reviewed"` alone. The mapping stays (ReviewStateKind has no
      "reviewed, disposition unknown" member and `rejected` is the worse guess) but it is
      a DECLARED DEFAULT under ambiguity, not an inference, and must say so. A false
      soundness claim about a default is precisely the failure this programme studies.
  (b) lift.py cites "the lossy-carrier note in docs/binding-contract.md" twice; no such
      note exists. Adding it — the contract document is where a binding's obligations
      already live, and it makes the citations true rather than deleting them.
  (c) Five of eight field-adders are asserted by nothing: Criticality, EvidenceAnchor,
      Entry, Provenance/Agent/Method could all be broken with all seven tests passing.
      Plus _add_basis's `kind == "claim"` filter is untested.
  Plan corrected in both places so a future run does not reproduce (a) or (b).
  Cost if wrong: comment prose and ~30 lines of test; no behaviour change to the lift.
Task 9: minor (deferred): Agent.kind=person is arbitrary, not safe — AgentKind has no
  least-committal member. Now declared as arbitrary in the lossy-carrier table.
Task 9: minor (deferred): test_lift_is_deterministic compares two calls in one process;
  real determinism would need a subprocess with a different PYTHONHASHSEED. Low risk —
  no sets are used internally and dict order is guaranteed.
Task 9: minor (deferred): unused URIRef import in lift.py. No linter configured.
Task 9: fix round 1/5 (3 addressed, 0 open — honest declared-default comment, real
  lossy-carrier note, 7 new tests; commits 5871d35..15b4d05)
Task 9: re-reviewer read ADR-002 itself rather than the report and confirmed the new
  comment is true; ran three live mutations (basis kind-filter removed, review-state
  default flipped to rejected, Agent.kind swapped to softwareAgent) and each killed its
  test. Fix diff purely additive — no prior assertion weakened.
Task 9: complete (commits fe667d7..15b4d05, review clean after 1 fix round)

Task 10: complete (commits 15b4d05..f2c1696, spec OK, quality approved)
Task 10: **the design's §3.4 gap is closed.** pyshacl was a declared dependency no module
  under src/ imported; it is now imported at validate.py:17 and `eaont validate` reports
  "shapes hold over 2 instances" over data the lift produced. The shapes have judged
  substrate-emitted data for the first time.
Task 10: gate proven to gate — five independent mutations by the reviewer: bogus standing
  refused (ClassConstraintComponent), bogus origin_class refused (mechanism generalises),
  removing the ontology parse makes CORRECT data fail with 12 violations (the four-argument
  contract is load-bearing), a planted bad fixture drives the CLI to `FAIL` + exit 1, and
  the new fixture-shape guard fires on Task 8's exact reintroduced defect.
Task 10: Ruling: carry the reviewer's finding 3 into Task 13's docs/limitations.md. A
  GovernedClaim stripped of status/criticality/provenance/entry/derivation ENTIRELY still
  conforms — GovernedClaimShape constrains only claimId/revision. Same documented boundary
  as ungrounded.json, reached from a new angle, and the asymmetry is worth stating: a
  marker leaves an auditable Unresolved node, silent omission leaves nothing to query. The
  fix would be in model/ontology.yaml, which Task 10 may not touch, so it is a limitation
  to declare rather than a defect to fix. Cost if wrong: one paragraph in limitations.md.
Task 10: minor (deferred): validate.py:5's docstring says pyshacl "is a declared dependency
  that no module under src/ imports" then imports it 12 lines later. Brief-verbatim; lift.py
  words the same point correctly in the past tense. Carrying into Task 13.
Task 10: minor (deferred): the fixture-shape guard is a pure-JSON lint living in a file
  whose docstring says "SHACL over real instances". Defensible placement, arguable.

Task 11: Ruling: one CRITICAL and two Important, all against plan-verbatim text, all fixed.
  CQ5 — the competency question for EA-REQ-14, the clause this programme turns on — could
  not detect silence. The reviewer deleted a claim's `derivation` key entirely (schema-
  conforming; `derivation` is not in the claim's `required`) and CQ5 still returned 2 rows,
  reporting the claim answerable. Cause: `bind:hasDerivation` is created by _add_basis and
  _add_evidence too, so the edge's presence says nothing about whether the derivation FIELD
  was answered. EA-REQ-14's rule is that a marker is a verdict and silence is not; the
  query could not tell resolved from marked from absent.
  **I relied on exactly this behaviour when I ruled on Task 8's blast radius** — I predicted
  CQ5 would still return 2 rows and it did. I was right about the outcome and wrong about
  what it meant: CQ5 passing was evidence of nothing. Recorded because the reasoning error
  is more useful than the fix.
  Fixed: each field must now be answered OR explicitly marked, and the derivation branch
  tests Derivation_expression rather than the shared node. CQ1 gains `?up a ea:GovernedClaim`
  (EA-REQ-02 asks for inputs that are "themselves resolvable claims"; a dangling reference
  passed). CQ2/CQ3 now bind per claim rather than counting rows.
  Plan corrected in all four places, plus Step 2's now-stale FAIL prediction.
  Cost if wrong: the CQ tests are stricter than the plan specified; no behaviour change.
Task 11: minor (deferred): docs/competency-questions.md cites docs/binding-contract.md for
  the basis/derivation node merge; the contract document does not document it. Third
  instance of a citation to content that does not exist — worth a final-review sweep.
Task 11: fix round 1/5 (4 addressed, 0 open; commits 702df03..3ee9fdb)
Task 11: re-reviewer ran four mutations including the one that matters most — replacing
  claim 1's derivation with a MARKER must still PASS, because a marker is a verdict. It
  does. The fix detects silence without rejecting what EA-REQ-14 permits.
Task 11: complete (commits f2c1696..3ee9fdb, review clean after 1 fix round)
Task 11: the binding-contract citation was DROPPED rather than back-filled — CQ5's note now
  grounds the basis/derivation node merge in lift.py's _derivation_node directly.
Task 11: minor (deferred): CQ4's evidence query still rests on the shared Derivation node,
  matching its own documented caveat. Out of that fix round's scope; worth a look at final.

Task 12: **THE FALSIFIER DID NOT FIRE. VERDICT: PASS, and it is earned.**
  Two claims about the same physical part carry genuinely different standings
  (engine1=verified, engine2=assumption). The ontology did NOT inherit ADR-010's limit:
  claim identity is a free-form string minted by the binding, and the ontology defines no
  attachment relation at all, so there is no population construct to collapse instances
  into and no ontology-owned edge that could be made to collapse them.
  ADR-010's obstacle is elsewhere and the reviewer read the ADR to confirm it: five named
  usages "the published model does not have", introducing them "would distort the model".
  That is a model-structure and editorial obstacle in SysML v2, not an expressiveness limit
  in the annotation vocabulary.
  **Proven falsifiable, not argued:** the reviewer monkeypatched a usage-anchored
  _add_status in-process and the test went red on the shipped fixture WITH distinct ids —
  so a collapsing attachment mechanism is not rescued by the identifier scheme. The test
  discriminates on exactly the property it claims to test.
  Consequence recorded: the SysML v2 binding inherits ADR-010's limitation and the ontology
  does not. That is a finding for the binding paper — what the language cannot carry.
  **Design §11.1's precondition on Phase 3 is discharged.**
Task 12: Ruling: three documentation-citation findings, all fixed, none touching the verdict.
  The most important is that docs/adr-010-test.md states ADR-010 "is not checked out on this
  machine". It is — at epistemic-adequacy-metamodel/docs/adr/010-*.md. In a document whose
  ONLY purpose is to prove a falsifier was run rather than assumed, a false statement about
  source availability is corrosive, and it is the one sentence a sceptical reader will test.
  Cost if wrong: prose in one record; the verdict and its evidence are unaffected.
Task 12: **PATTERN, now four instances: citations to content that does not exist or does not
  resolve.** lift.py x2 (a lossy-carrier note that had not been written), competency-questions
  (a merge binding-contract.md never documented), and now adr-010-test.md citing "design §4",
  which in the reachable in-repo design doc is "Divergence resolutions" — a reader following
  it lands on the wrong section. Root cause: this repo cites a split design that lives in
  research-programme and is not vendored, so bare section numbers resolve against the WRONG
  document. The final whole-branch review must sweep for this class specifically.
Task 12: fix round 1/5 (3 addressed, 0 open; commits c44c304..4d3d75a). Re-reviewer read
  ADR-010 at its real path and confirmed the quote is character-for-character accurate.
  Verdict untouched — the diff carries no test logic, no fixture, no VERDICT line.
Task 12: complete (commits 3ee9fdb..4d3d75a, review clean after 1 fix round)

Task 13: Ruling: **the split design's §8 was FALSE WHEN WRITTEN, and it is my error.**
  It claimed a grep for SACM/assurance-case/Toulmin/nanopublication across the spec,
  metamodel and publications repos "returns nothing outside .venv and .git noise".
  publications/epistemic-adequacy-probe/source/refs.bib has carried @techreport{OMGSACM}
  and @article{Kuhn2016Nanopublications} since d6c6b77 on 2026-08-19 — two days BEFORE the
  design — and the NIER paper's .tex and PDF cite SACM too. The grep was piped through
  `head -20` and all twenty lines were .venv noise, so truncation hid every real hit.
  Corrected in the design with a dated correction block (841ed81). The positioning task
  survives; its justification was overstated, and the truth is better news — the accepted
  paper already engaged this literature, so related-work.md builds on its citations rather
  than starting cold.
  **This is the SECOND false negative this session from a truncated grep.** The first was
  "pyshacl is imported nowhere", falsified by tests/test_generate_shacl.py:62. Both became
  load-bearing claims in a design document. Standing lesson: a grep piped through `head` is
  not evidence of absence, and an absence claim needs an untruncated command.
  Cost if wrong: none — the correction is strictly more accurate than what it replaced.
Task 13: Ruling: warrant.md's ADR-008 quote and ADR-002's dead link are IN scope for a fix
  round even though outside the task's declared files. "Outside my files" is not a reason to
  leave a misquote standing when you are the one who found it. ADR-008 actually reads
  "@GovernedClaim is a population declaration, not epistemic metadata"; warrant.md (and I,
  repeatedly) quoted only the negative half, dropping what the entity IS — in the one
  document whose contribution is classifying entities by what warrants them.
  Cost if wrong: a longer table cell; test_warrant.py parses that file by string index and
  the implementer has been told to report rather than trim the quote to fit the parser.
Task 13: fix round 1/5 (3 addressed, 0 open; commits b506e3a..d4e3a68). The link sweep
  found FIVE dead links, not the one flagged — three ADRs to ../round-trip-loss.md and two
  in the retained design doc to the reader-spike record. All five targets verified to exist
  at their new repository+path before the links were written. The ADR-008 quote was NOT
  trimmed to fit the parser; the re-reviewer verified _table_rows is front-anchored and
  length-insensitive.
Task 13: complete (commits 4d3d75a..d4e3a68, review clean after 1 fix round)
Task 13: minor (deferred): docs/adr/006 still names the schema metamodel/metamodel.yaml.
  Deliberately unchanged — ADRs are extracted, not composed, and renaming inside one edits
  the record. Open question for the programme: should ADRs track renames?
Task 13: minor (deferred): the SACM comparison table is prose-only; nothing fails if SACM
  2.4 changes AssertionDeclaration. The document says so itself.
Task 13: minor (deferred): docs/adr-010-test.md:213 forward-references
  epistemic-adequacy-sysml-v2-binding, which does not exist until Phase 3's rename. Fifth
  instance of the citation pattern, though this one is a deliberate forward reference.
Task 13: the implementer's closing observation, worth keeping: **both false negatives this
  round were TRUNCATION — a quote cut at the halfway point, a grep cut at twenty lines.
  Neither announces itself: a cut quote still reads as a quote, a truncated grep still
  exits zero.**

## All 13 tasks complete. Final whole-branch review next.

## Final whole-branch review

Verdict: **delivers its claim. No Critical findings.** Five Importants + a citation sweep,
all fixed in one wave (6cca047, 3cc1a79, 2e724c8, f6ead17) and all six ADDRESSED on
re-review, with no ADR body edited and no new breakage.

The headline finding is one no per-task gate could see: SysML v2 shape surviving in the
PUBLISHED RDF PROSE. The types were agnostic; the comments were not. generate/owl.py
emitted an rdfs:comment into prov-alignment.ttl citing `bindings/sysml-v2.md §6` — a SysML
binding document absent from this repository — so the language-agnostic vocabulary carried
a dangling citation to a binding inside its own provenance node. Alongside it: @Unresolved
and @Provenance (the @ sigil is SysML v2 annotation syntax), "model tree", "substrate root
package", "applied by the extractor". A JSON binding has no packages, no @ annotations and
no extractor. Task 2 handled types; Task 6 rewrote enumeration member docs; nobody owned
entity-level prose.

Ruling: park the one residual nit. docs/adr/README.md's opening still says "Every decision
below was already made and argued in [the in-repo design]" while the new paragraph below it
names ADR-012 as the exception — a reader meets a brief contradiction before the caveat.
Real but not load-bearing, and rewriting the opener exceeds the one-paragraph fix that was
scoped. Cost if wrong: a moment's confusion in an index file.

## Done. 13 tasks, 21 commits, 101 tests, 8 fix rounds, 0 Criticals at final review.
